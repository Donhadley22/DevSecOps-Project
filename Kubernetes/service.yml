apiVersion: v1
kind: Service
metadata:
  name: netflix-app
  namespace: netflix-app
spec:
  type: LoadBalancer
  selector:
    app: netflix-app
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
